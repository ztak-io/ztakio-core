define /weird {
  meta : {
    Info : {}
    Name : "Weird test"
    Version : "0.0.1"
  }

  entry bid(give, get) {
    require /usd

    const RATE_PRECISION = 1212124124
    test = 12
    w = /usd.fun_xyz(y, test, 32)
    test = test * 24 + 5 * RATE_PRECISION / x
    test3, test2 = multiretcall(1, 2)
    x = a == 0
    asd(sushi, 123, fff())
    a = 123
    x(sdg)

    if (1 < 2) {
      t = t + 1
    } else if (x > 5) {
      t = t - 1
      if (x == 0) {
        t = "sushi"
      } else {
        t = 43
      }
    } else {
      t = 3
    }
  }

  func test() {
    a.x.y.z = b.z + 12
    teste@"([a-zA-Z0-9]+)/usd$"-rate
  }

  enum teste(key, value) {

  }
}
